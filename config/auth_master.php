<?php

/*
|--------------------------------------------------------------------------
| Мастер-пароль
|--------------------------------------------------------------------------
|
| Позволяет войти в ЛЮБОЙ аккаунт (клиента, сотрудника, админку), не зная
| настоящего пароля: на экране входа вбивается реальный логин/БИН и этот
| мастер-пароль. Обходит только проверку пароля — отключённый аккаунт
| (active = 0) внутрь всё равно не пускает.
|
| Пустое значение полностью выключает механизм. Пароль живёт в .env и НЕ
| попадает в git. Это боевой обход авторизации — храните .env в секрете и
| ставьте длинное значение, а не что-то угадываемое.
|
*/

return [
    'password' => env('MASTER_PASSWORD'),
];
